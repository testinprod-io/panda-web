{
  "name": "panda-vault",
  "version": "1.0.0",
  "description": "Panda cryptographic vault - sandboxed iframe for secure key operations",
  "main": "vault.js",
  "type": "module",
  "scripts": {
    "build": "tsc && cp index.html dist/",
    "dev": "npm install && npm run build && serve -l 3001 dist --cors='*'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "serve": "^14.2.3",
    "typescript": "^5.0.0"
  },
  "keywords": ["cryptography", "security", "sandbox", "iframe"],
  "author": "Panda Team",
  "license": "PROPRIETARY"
}